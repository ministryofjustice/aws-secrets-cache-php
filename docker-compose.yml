---
version: "3.6"

services:
  composer:
    image: composer
    volumes:
      - "~/.composer:/tmp"
      - "./:/app"
    command: ["install", "--ignore-platform-reqs"]

  test-runner:
    build:
      context: .
      dockerfile: test.Dockerfile
    volumes:
      - "./:/app"
