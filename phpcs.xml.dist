<?xml version="1.0"?>
<ruleset>
    <!-- display progress -->
    <arg value="p"/>
    <arg name="colors"/>

    <!-- inherit rules from: -->
    <rule ref="PSR12"/>

    <!-- Paths to check -->
    <file>.</file>

    <!-- Exclude vendor files -->
    <exclude-pattern>*/vendor/*</exclude-pattern>
</ruleset>
